import { describe, expect, it } from 'vitest'

// Scaffolding only: proves the test harness runs before any real code exists.
// Delete this file once tests/core/money.test.ts lands in M1.
describe('test harness', () => {
  it('runs TypeScript tests', () => {
    const cents: number = 100
    expect(cents).toBe(100)
  })
})
